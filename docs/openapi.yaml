openapi: 3.0.3
info:
  title: Flexibility Products API (minimal test)
  version: "1.0.0"
paths:
  /services:
    get:
      summary: List services
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
