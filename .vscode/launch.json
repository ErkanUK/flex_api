{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open docs in Browser",
      "type": "pwa-browser",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}